# Aponta o front para a API do SMSMais.server.
# Em dev, por padrão o Vite faz proxy /api -> http://localhost:5080 (ver vite.config.ts),
# então você pode deixar a variável abaixo vazia/comentada.
# VITE_API_BASE_URL=http://localhost:5080
