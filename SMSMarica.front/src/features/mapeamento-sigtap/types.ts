export type PendenteMapeamento = {
  sigtapCodigo: string;
  procedimentoTexto: string | null;
  quantidade: number;
};

export type TipoExameOpcao = { id: string; nome: string };

export type VincularResultado = { atualizados: number };
