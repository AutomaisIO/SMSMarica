/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Carimbo do build, injetado pelo vite.config (SHA no CI, timestamp local). */
declare const __VERSAO_BUILD__: string;
