# Failover pela Connect (ether2) - deteccao por estado fisico da ether1 (Prefeitura)
# Regras FAILOVER (nat/takeover) sao criadas NO SITE com os dados L3 reais da LAN;
# o script enable/disable por comentario — sem regras, e no-op seguro.
/ip dhcp-client add interface=ether2 comment="Connect - internet do MK" disabled=no use-peer-ntp=no use-peer-dns=no
/system script add name=FAILOVER-ether1 policy=read,write source=":global foState; :global foCnt; :if ([:typeof \$foState] = \"nothing\") do={ :set foState \"off\" }; :if ([:typeof \$foCnt] = \"nothing\") do={ :set foCnt 0 }; :local up [/interface get [find name=ether1] running]; :if (\$up = false) do={ :if (\$foState = \"off\") do={ :set foCnt (\$foCnt + 1); :if (\$foCnt >= 2) do={ /ip firewall nat enable [find comment~\"FAILOVER\"]; /ip dns set allow-remote-requests=yes; /ip address enable [find comment~\"FAILOVER gw takeover\"]; :set foState \"on\"; :set foCnt 0; :log warning \"FAILOVER-ether1 ATIVADO\" } } else={ :set foCnt 0 } } else={ :if (\$foState = \"on\") do={ :set foCnt (\$foCnt + 1); :if (\$foCnt >= 2) do={ /ip address disable [find comment~\"FAILOVER gw takeover\"]; /ip firewall nat disable [find comment~\"FAILOVER\"]; /ip dns set allow-remote-requests=no; :set foState \"off\"; :set foCnt 0; :log warning \"FAILOVER-ether1 DESATIVADO\" } } else={ :set foCnt 0 } }"
/system scheduler add name=FAILOVER-check interval=5s on-event="/system script run FAILOVER-ether1" comment="failover Connect"
